A Simple Next.JS application to power personal blog. 

### Installation

- Clone the repo
- Run `yarn install`
- Add .env variables `NEXT_PUBLIC_GITHUB_ISSUES_URL` and `NEXT_PUBLIC_GITHUB_API_KEY`
- Run the dev server using `yarn dev`